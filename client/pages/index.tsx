import { Box } from 'rebass';

import { Layout } from '../components/Layout';

const Home = () => {
  return (
    <Layout>
      <Box width={1 / 2} p={3}></Box>
    </Layout>
  );
};

export default Home;
